package Lotto;

import java.util.*;

public class DraftDriver {

	BallFactory b1 = new BallFactory();
	
	List<Ball> BList = new ArrayList<>(1001);
	
	
		
		
		
	public DraftDriver(BallFactory b1, List<Ball> bList) {
		super();
		this.b1 = b1;
		BList = bList;
	}





	public Ball Winner(){	

		 String[] text = { "s1", "s2", "s3", "s4", "s5",
		 "s6", "s6", "s7", "s8", "s9", "s10", "s11", "s12", "s13",
		 "s14" };
		 int[] numBalls = { 250, 199, 156, 119, 88, 63, 43, 28, 17, 11, 8, 7,
		 6, 5};
		
		 for (int i = 0; i < numBalls.length; i++) {
		 BList.addAll(b1.createBall(numBalls[i], text[i]));
		 }
		 Hopper nba = new Hopper();
		 nba.shuffle(BList);
		 System.out.println(BList.get(0));
		 BList.remove(0);
//		for (int i = 1; i < text; i++) {
//			nbaBalls.addall(b1.createBall(Integer.toString(i)));
//
//		}
//
//		for (int i = 1; i < 26; i++) {
//			nbaBalls2.add(b1.createBall(Integer.toString(i)));
//		}



//			for (int h = 0; h < 1; h++) {
//				nba.shuffle(nbaBalls2);
//				System.out.println("Red: " + nbaBalls2.get(0));

				//for (int j = 0; j < 5; j++) {
					
			

				

			//}

			// System.out.println(nbaBalls + "\n");
	}		// System.out.println(nbaBalls.size());
		
}
	
}
