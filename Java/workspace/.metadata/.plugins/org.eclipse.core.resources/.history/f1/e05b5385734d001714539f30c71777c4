package Jets;

public class SR71 extends Jets {

	public SR71(String model, float speed, float range, int capacity) {
		super(model, speed, range, capacity);
		setCapacity(capacity);
		setModel(model);
		setRange(range);
		setSpeed(speed);
		
	}

}
